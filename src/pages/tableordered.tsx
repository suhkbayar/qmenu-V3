import React from 'react';
import { Notfound } from '../components';

const NotFoundPage: React.FC = () => {
  return <Notfound title="Ширээг урьдчилан захиалсан байна." />;
};

export default NotFoundPage;
